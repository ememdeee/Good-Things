// @prepros-prepend "tab-group-common.js";

// Wait until DOM loaded
document.addEventListener('DOMContentLoaded', function () {

    // Call initializeTabGroup() right after DOM is loaded
    initializeTabGroup();
	// console.log('TabGroup initialized');

});
