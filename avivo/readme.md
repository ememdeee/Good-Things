npm run sass:theme       # Build just theme.scss
npm run sass:blocks      # Build just blocks
npm run watch:theme      # Watch just theme.scss
npm run watch:blocks     # Watch just blocks

npm run sass:watch

npm run build